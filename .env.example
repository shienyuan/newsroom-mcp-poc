# Azure OAuth Configuration
# Get these values from your Azure App Registration:
# 1. Go to Azure Portal → Microsoft Entra ID → App registrations
# 2. Select your app or create a new one
# 3. Copy the Application (client) ID and Directory (tenant) ID from Overview
# 4. Go to Certificates & secrets → New client secret → Copy the secret value
#
# Note: This server uses a patched Azure provider that removes the 'resource' parameter
# for Azure AD v2.0 compatibility. See AZURE_V2_RESOURCE_PARAMETER_FIX.md for details.

# Application (client) ID from Azure App Registration
FASTMCP_SERVER_AUTH_AZURE_CLIENT_ID=your-application-client-id-here

# Client secret value from Azure App Registration
FASTMCP_SERVER_AUTH_AZURE_CLIENT_SECRET=your-client-secret-value-here

# Directory (tenant) ID from Azure App Registration
FASTMCP_SERVER_AUTH_AZURE_TENANT_ID=your-directory-tenant-id-here

# Base URL where your server is running
# For local development: http://localhost:8000
# For production: https://your-domain.com
FASTMCP_SERVER_AUTH_AZURE_BASE_URL=http://localhost:8000

# OAuth callback path (must match Azure App Registration redirect URI)
FASTMCP_SERVER_AUTH_AZURE_REDIRECT_PATH=/auth/callback

# Required OAuth scopes (comma-separated)
# Note: User.Read is required for FastMCP to validate tokens via Microsoft Graph API
FASTMCP_SERVER_AUTH_AZURE_REQUIRED_SCOPES=User.Read,email,openid,profile

# OAuth timeout in seconds (default: 10)
FASTMCP_SERVER_AUTH_AZURE_TIMEOUT_SECONDS=10

